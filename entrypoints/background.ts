export default defineBackground(() => {
  // P1.3 wires offscreen lifecycle + message hub here. Bootstrap stub only.
  console.log('[smartdeal-hunter] background ready', { id: browser.runtime.id });
});
